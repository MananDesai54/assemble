# Slack user token (api.slack.com -> your app -> OAuth & Permissions -> User OAuth Token)
# Reads the channels/DMs you are in, as you. Needs *:history + *:read + chat:write user scopes.
SLACK_USER_TOKEN=
# local server port (assemble daemon)
ASSEMBLE_PORT=
