{
  "name": "@assemble/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "@assemble/actions": "workspace:*",
    "@assemble/core": "workspace:*",
    "@assemble/integration-slack": "workspace:*",
    "@assemble/integration-linear": "workspace:*",
    "@assemble/llm": "workspace:*",
    "@assemble/stt": "workspace:*",
    "hono": "^4.0.0"
  }
}
